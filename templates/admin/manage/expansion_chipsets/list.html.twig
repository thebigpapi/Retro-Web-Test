{% if objectList is not empty %}
    {{ knp_pagination_render(objectList, 'macros/pagination.html.twig') }}
    <div class="plain-list w300p">
        {% if entityName == "expansionchip" %}<div class="list-entry centered" style="font-style:italic;display:none;">
            <div>Manufacturer</div>
            <div>Name</div>
            <div>Type</div>
            <div>Part number</div>
            <div>Actions</div>
        </div>{% endif %}
        {% if entityName == "expchiptype" %}<div class="list-entry centered" style="font-style:italic;display:none;">
            <div>Name</div>
            <div>Actions</div>
        </div>{% endif %}
        {% for object in objectList %}
            {% include 'admin/manage/expansion_chipsets/' ~ entityName ~ '.html.twig' with {'object':object} %}	
        {% endfor %}
    </div>
    {{ knp_pagination_render(objectList, 'macros/pagination.html.twig') }}
{% endif %}