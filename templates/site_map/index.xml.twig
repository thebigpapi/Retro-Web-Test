<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for subfile in sub_files %}
    <sitemap>
        <loc>{{ absolute_url(subfile) }}</loc>
        <lastmod>{{ mod_date }}</lastmod>
    </sitemap>
    {% endfor %}
</sitemapindex>
